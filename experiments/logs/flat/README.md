# flat experiment logs

Each experiment should use a timestamped folder directly under this directory:

- `run_<timestamp>/`

Per-run required subfolders:

- `native_traffic/`
- `transformed_traffic/`
- `route_logs/`
- `sim_logs/`
- `summary/`
